\documentclass[noanswers]{exam}
\usepackage{texPreamble}
\usepackage{subfiles}
\newcount\examUnit
\examUnit=0 %formula sheet only prints for nonzero values
\newcount\classNum
\classNum=0 %used for class specific directions

\begin{document}
\ifprintanswers
  \begin{center}
    \fbox{\parbox{0.8\linewidth}{
        \begin{flushleft}
          \textbf{Grading Guidelines:}
          \begin{itemize}
            \item Graded out of \numpoints\xspace total points
            \item A max deduction per question of \sfrac{1}{2} for improper notation
            \item Equivalent/unsimplified answers accepted
            \item Blank questions/nonsense answers receive no credit
          \end{itemize}
        \end{flushleft}
      }}
  \end{center}
\else
  \begin{center}
    \fbox{\fbox{\parbox{0.8\linewidth}{
      \parbox{0.95\linewidth}{
        \begin{flushleft}
          \begin{itemize}
            % \setlength{\itemsep}{0.125\baselineskip}
            \item You must show \textbf{all} work and use correct notation to receive full credit.
            \item Illegible responses are not graded.
            \item If multiple answers are provided, the incorrect one(s) will be graded.
            \item Scratch paper will \textbf{not} be graded.
            \item You may \textbf{not} discuss this exam with anyone else until the grades are released.
          \end{itemize}
          \textbf{~One bonus point will be included for following all of these directions:}
          \begin{itemize}
            % \setlength{\itemsep}{0.125\baselineskip}
            \item You may separate the formula sheet from the rest of the exam but you \textbf{must} return all pages of this exam in the correct order.
            \item Please place your name on every name line (top of \textbf{every} odd page).
            \item Place your final answer in the corresponding box when provided.
              \ifnum\classNum=211
              \item You may only use ``StatCrunch'', the ``Rossman Chance'' applets, or a scientific calculator.
            \else
              \item You may use a scientific calculator.
            \fi
            No other electronics are to be used.
            % \item \textsc{Include supporting work!} Partial credit can't be given otherwise!
          \end{itemize}
        \end{flushleft}
      }}}}
  \end{center}

  \pagebreak
  \ifnum\examUnit=0
  \else
    \subfile{formula_sheet}
  \fi
  \pagebreak
\fi
\end{document}